Place backend patient OpenAPI spec at:

- `openapi/patient.json`

Then run:

```bash
npm run generate:types
```

Expected output:

- `src/types/generated/patient-openapi.generated.ts`

Current app contracts are maintained in:

- `src/types/generated/patient-api.ts`
